---
"@fairground-co/core": minor
---

Lookbook: a self-contained specimen catalog (`dist/lookbook.html`) showing
every token with its live resolved value and every primitive interactively —
plus `scripts/build-lookbook.mjs`, which theme repos use to generate their own
branded lookbook (theme CSS + voice doc inlined, neutral ⇄ brand switcher).
