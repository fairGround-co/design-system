---
"@fairground-co/core": minor
---

Initial core package (Phase 1 partition-import, issue #2): token contract v0
as CSS custom properties + TS types, neutral reference theme (light + dark +
density axis), and 33 generic primitives + 2 hooks genericized from the seed
system (contract tokens only; scope-colored capability signals; brand presets
moved app-side).
